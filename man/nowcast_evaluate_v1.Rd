% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nowcast_evaluate.R
\name{nowcast_evaluate_v1}
\alias{nowcast_evaluate_v1}
\title{Score nowcast methods on interval coverage and revision}
\usage{
nowcast_evaluate_v1(
  triangle,
  methods,
  max_delay_days,
  as_of_weeks = NULL,
  horizons = 1:2,
  probs = c(0.025, 0.05, 0.1, 0.25, 0.5, 0.75, 0.9, 0.95, 0.975),
  by = "horizon",
  thresholds = c(0.25, 0.5),
  seed = NULL
)
}
\arguments{
\item{triangle}{A \code{csfmt_reporting_triangle_v3} with one series.}

\item{methods}{One method, or a named list of methods. A method takes a
triangle and returns a \code{csfmt_ensemble_v3}. One method gets the name
\code{"method"}.}

\item{max_delay_days}{The delay horizon in days.}

\item{as_of_weeks, horizons, probs, seed}{Passed to \code{\link[=nowcast_backtest]{nowcast_backtest()}}. \code{probs}
MUST include 0.05, 0.25, 0.5, 0.75 and 0.95.}

\item{by}{The columns to group the scores by.}

\item{thresholds}{The absolute revisions for the \verb{p_gt_<t>} columns.}
}
\value{
A data.table with one row per group and method:
\itemize{
\item \code{n}: the number of scored forecasts,
\item \code{coverage_50}, \code{coverage_90}: the share of truths from the 0.25 to the 0.75
quantile, and from the 0.05 to the 0.95 quantile,
\item \code{median_signed}, \code{median_abs}, \code{q05}, \code{q95}: the median revision, the median
absolute revision, and the 5\% and 95\% quantiles of the revision,
\item \verb{p_gt_<t>}: the share of absolute revisions above each threshold, such as
\code{p_gt_25} for 0.25,
\item \code{method}.
}

A method with no nowcast gives a warning and no rows.
}
\description{
Replays each method with \code{\link[=nowcast_backtest]{nowcast_backtest()}}, and scores every nowcast against
the settled truth from \code{\link[=nowcast_truth]{nowcast_truth()}}. It returns a row for each horizon and
method.
}
\details{
The revision is \code{(median - truth) / truth}, over the weeks with a truth above
0. Every score is a measurement on the replayed weeks, not a property of a
method. The methods replay the same as-of dates with the same \code{seed}, so they
are paired by forecast. That does not give common random numbers, which would
also need the methods to use their random numbers in the same way.
}
\examples{
# a small reporting triangle: 30 weeks, each reported 3, 10 and 17 days
# after its Monday
monday <- as.Date("2023-01-02") + 7 * rep(0:29, each = 3)
d <- data.table::data.table(
  isoyearweek_reference = format(monday, "\%G-\%V"),
  reporting_date = monday + rep(c(3, 10, 17), 30),
  numerator = 10, indicator = "x", location = "n", age = "total", sex = "total")
tri <- csfmt_reporting_triangle_v3(d, id_cols = c("indicator", "location", "age", "sex"))

# one method
nowcast_evaluate_v1(tri, function(x) nowcast_passthrough_to_ensemble_v1(x, max_delay_days = 21),
                    max_delay_days = 21, horizons = 0:2, seed = 1)
# a named list of methods, with a `method` column in the result
nowcast_evaluate_v1(tri, max_delay_days = 21, horizons = 0:2, seed = 1, methods = list(
  passthrough = function(x) nowcast_passthrough_to_ensemble_v1(x, max_delay_days = 21)))
}
\seealso{
\code{vignette("pipeline", package = "csalert")}, stage 2, which explains
how to read each column.

Other nowcast diagnostics:
\code{\link[=nowcast_backtest]{nowcast_backtest()}},
\code{\link[=nowcast_censor]{nowcast_censor()}},
\code{\link[=nowcast_truth]{nowcast_truth()}}
}
\concept{nowcast diagnostics}
