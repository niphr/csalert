% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nowcast_revision.R
\name{nowcast_revision}
\alias{nowcast_revision}
\title{Point-estimate revision of a nowcast backtest, by horizon}
\usage{
nowcast_revision(backtest, truth, by = "horizon", thresholds = c(0.25, 0.5))
}
\arguments{
\item{backtest}{Output of [nowcast_backtest] (long quantile nowcasts).}

\item{truth}{Output of [nowcast_truth] (settled totals per reference week).}

\item{by}{Grouping columns for the summary (default "horizon").}

\item{thresholds}{Absolute-revision cut-offs to report the exceedance
probability for (default 25\% and 50\%).}
}
\value{
One row per group: `n`, `median_signed` (bias), `median_abs`,
  `q05`/`q95` (the 5-95\% revision band), and `p_gt_<t>` for each threshold.
}
\description{
Point-estimate revision of a nowcast backtest, by horizon
}
