% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/naming_grammar.R
\name{q_label}
\alias{q_label}
\title{Write a probability as a quantile label}
\usage{
q_label(p)
}
\arguments{
\item{p}{A numeric vector of probabilities.}
}
\value{
A character vector of labels, with \code{NA} for \code{NA}.
}
\description{
Writes \code{q}, two integer-percent digits, \code{x} and one decimal-percent digit, so
\code{0.025} becomes \code{"q02x5"}.
}
\details{
The label loses information in two ways. A finer probability is rounded:
\code{0.0125} becomes \code{"q01x2"}. And \code{p = 1} gives \code{"q100x0"}, which \code{\link[=q_value]{q_value()}}
cannot read. Keep \code{p} on the 0.001 grid and below 1.
}
\examples{
q_label(c(0.025, 0.5, 0.975))

# limit 1: a probability finer than one decimal percent is rounded
q_value(q_label(0.0125))

# limit 2: p = 1 gives a three-digit label, and q_value() returns NA for it
q_label(1)
q_value(q_label(1))
}
\seealso{
\code{vignette("pipeline", package = "csalert")}, whose naming-grammar
section shows both limits.

Other naming grammar functions:
\code{\link[=csfmt_interpret]{csfmt_interpret()}},
\code{\link[=csfmt_parse]{csfmt_parse()}},
\code{\link[=csfmt_var]{csfmt_var()}},
\code{\link[=q_value]{q_value()}}
}
\concept{naming grammar functions}
