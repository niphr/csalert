% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nowcast_calibration.R
\name{nowcast_estimate_calibration_v1}
\alias{nowcast_estimate_calibration_v1}
\title{Estimate an interval scaling factor from a backtest}
\usage{
nowcast_estimate_calibration_v1(backtest, truth, level = 0.9, by = "horizon")
}
\arguments{
\item{backtest}{The output of \code{\link[=nowcast_backtest]{nowcast_backtest()}}. The function uses the
\code{quantile_level} nearest to each end of the interval and to the median.}

\item{truth}{The output of \code{\link[=nowcast_truth]{nowcast_truth()}}.}

\item{level}{The central interval level.}

\item{by}{The columns that the factor varies over.}
}
\value{
A \code{nowcast_calibration}: a list with \code{level}, \code{by} and \code{table}, which
has the \code{by} columns, \code{n}, \code{coverage_raw}, the share of truths inside the
interval, and \code{factor}.
}
\description{
Measures, for each group, the factor that would have made the central \code{level}
interval of past nowcasts cover \code{level} of their settled truths. Use it to
check an engine. \code{\link[=nowcast_apply_calibration_v1]{nowcast_apply_calibration_v1()}} applies it if you choose to.
}
\details{
The factor is the type-7 \code{level} quantile of \verb{|truth - median| / halfwidth},
where \code{halfwidth} is half the width of the interval. Above 1, the intervals
were too narrow, and below 1 too wide.

This is an empirical rescaling, not split conformal. It uses the type-7
quantile, not the order statistic that a conformal argument needs, and one
symmetric distance for both tails. So it carries NO finite-sample coverage
guarantee. \code{coverage_raw} is what the engine did on these replayed weeks, not a
property of the engine.
}
\examples{
# 40 reference weeks, each reported 3, 10 and 17 days after its Monday
monday <- as.Date("2023-01-02") + 7 * rep(0:39, each = 3)
set.seed(1)
d <- data.table::data.table(
  isoyearweek_reference = format(monday, "\%G-\%V"),
  reporting_date = monday + rep(c(3, 10, 17), 40),
  numerator = rpois(120, c(30, 15, 5)),
  indicator_tag = "x", location_code = "nation", age = "total", sex = "total"
)
d <- d[reporting_date <= as.Date("2023-01-02") + 7 * 39 + 6]
tri <- csfmt_reporting_triangle_v3(
  d,
  id_cols = c("indicator_tag", "location_code", "age", "sex")
)

method <- function(x) nowcast_delay_ecdf_v1(x, max_delay_days = 21, n_sim = 200)
bt <- nowcast_backtest(
  tri, method,
  max_delay_days = 21,
  as_of_weeks = as.Date("2023-01-02") + 7 * (20:38) + 6,
  horizons = 0:1, seed = 1
)

# The two horizons fall on opposite sides of 0.9, on 17 and 18 scored weeks.
# That is too little evidence to call the engine over- or under-dispersed, so
# read a factor as a reason to look closer, not as a verdict.
nowcast_estimate_calibration_v1(bt, nowcast_truth(tri, max_delay_days = 21))
}
\seealso{
\code{vignette("pipeline", package = "csalert")}, stage 2, which measures
the coverage that the factor corrects.

Other nowcast calibration functions:
\code{\link[=nowcast_apply_calibration_v1]{nowcast_apply_calibration_v1()}},
\code{\link[=print.nowcast_calibration]{print.nowcast_calibration()}}
}
\concept{nowcast calibration functions}
