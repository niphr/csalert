% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nowcast_calibration.R
\name{nowcast_apply_calibration}
\alias{nowcast_apply_calibration}
\title{Apply a nowcast calibration to quantile predictions}
\usage{
nowcast_apply_calibration(x, calibration)
}
\arguments{
\item{x}{Long quantile predictions (`reference`, the calibration's `by`
column(s), `quantile_level`, `predicted`) -- e.g. a fresh [nowcast_backtest]
output or a melted collapse.}

\item{calibration}{A `nowcast_calibration` from [nowcast_estimate_calibration].}
}
\value{
`x` with `predicted` recalibrated.
}
\description{
Recalibrates each quantile by scaling its distance from the median by the
learned per-group `factor`, so the intervals hit nominal coverage. Groups with
no learned factor (e.g. an unseen horizon) pass through unchanged.
}
