% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rate.R
\name{ens_add_rate}
\alias{ens_add_rate}
\alias{ens_add_rate.csfmt_ensemble_v3}
\title{Add a rate to an ensemble}
\usage{
ens_add_rate(x, ...)

\method{ens_add_rate}{csfmt_ensemble_v3}(x, numerator, denominator, per = 100, name = NULL, ...)
}
\arguments{
\item{x}{The \code{csfmt_ensemble_v3} that holds both measures.}

\item{...}{Passed to the method.}

\item{numerator, denominator}{Two draw matrices in \verb{$draws}.}

\item{per}{The scale of the rate. \code{100} gives a percentage.}

\item{name}{The name of the new measure. \code{NULL} uses
\code{csfmt_var(numerator, denom = denominator, per = per)}.}
}
\value{
\code{x} with the rate in \verb{$draws}.
}
\description{
Divides one draw matrix by another, draw by draw, and adds the result as a new
draw matrix. Column \code{j} is the same draw in every measure, so the rate carries
the uncertainty of both.
}
\details{
A zero denominator gives \code{NA}, not 0. The rate is capped at \code{per}, because the
numerator is a subset of the denominator, and a draw above the cap gives a
warning.
}
\examples{
d <- data.table::data.table(
  location_code = "nation",
  age = "total",
  isoyearweek = c("2023-01", "2023-02", "2023-03")
)
# The numerator is a SUBSET of the denominator: positive tests out of tests
# taken. So simulate the denominator first, and the numerator from it.
set.seed(1)
denom <- matrix(rpois(3 * 100, 200), nrow = 3)
numer <- matrix(rbinom(length(denom), size = denom, prob = 0.10), nrow = 3)
ens <- csfmt_ensemble_v3(
  d,
  id_cols = c("location_code", "age"),
  draws = list(
    numerator_nowcasted = numer,
    denominator_nowcasted = denom
  )
)

ens <- ens_add_rate(
  ens,
  numerator = "numerator_nowcasted",
  denominator = "denominator_nowcasted",
  per = 100
)

# the rate is a third draw matrix, named by the grammar
names(ens$draws)

# its interval carries the uncertainty of both measures
r <- ens_collapse(ens, probs = c(0.05, 0.5, 0.95))
r[, .(
  isoyearweek,
  lo = numerator_nowcasted_vs_denominator_nowcasted_pr100_q05x0,
  med = numerator_nowcasted_vs_denominator_nowcasted_pr100_q50x0,
  hi = numerator_nowcasted_vs_denominator_nowcasted_pr100_q95x0
)]
}
\seealso{
\code{vignette("pipeline", package = "csalert")}, stage 4.

Other ensemble operations:
\code{\link[=ens_collapse]{ens_collapse()}},
\code{\link[=mem_thresholds_v1]{mem_thresholds_v1()}},
\code{\link[=short_term_trend]{short_term_trend()}},
\code{\link[=signal_detection_hlm]{signal_detection_hlm()}}
}
\concept{ensemble operations}
