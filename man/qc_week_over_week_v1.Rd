% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{qc_week_over_week_v1}
\alias{qc_week_over_week_v1}
\title{Compare this week's run with last week's run}
\usage{
qc_week_over_week_v1(
  current,
  previous,
  max_delay_weeks,
  tol = 1e-06,
  status_roles = c("status", "hlmstatus")
)
}
\arguments{
\item{current, previous}{The output of \code{\link[=ens_collapse]{ens_collapse()}} for this run and for the
previous run. Their identity columns MUST have the names that
\code{\link[=compare_results]{compare_results()}} needs.}

\item{max_delay_weeks}{The nowcast horizon in ISO weeks. The engines take
\code{max_delay_days}, in days. The names differ so that one name never means two
units.}

\item{tol}{The largest change in a settled median that counts as no change.}

\item{status_roles}{The roles that hold an ordinal status. \verb{$signal} holds only
these roles, and \verb{$integrity} leaves them out. The default holds \code{"status"}
from \code{\link[=mem_thresholds_v1]{mem_thresholds_v1()}} and \code{"hlmstatus"} from \code{\link[=signal_detection_hlm]{signal_detection_hlm()}}.}
}
\value{
A list of two data.tables:
\itemize{
\item \code{integrity}, with \code{indicator_tag}, \code{isoyearweek}, \code{column}, \code{prv}, \code{cur}
and \code{abs_diff},
\item \code{signal}, with \code{indicator_tag}, \code{isoyearweek}, \code{column}, \code{from}, \code{to} and
\code{change}, which is \code{"new"} or \code{"changed"}.
}

A table with no rows keeps \code{prv} and \code{cur}, and has no added column.
\code{indicator_tag} is there only when the input has it.
}
\description{
Splits the output of \code{\link[=compare_results]{compare_results()}} at the nowcast horizon. \verb{$integrity}
lists the settled weeks whose median changed, and \verb{$signal} lists the status
changes in the newer weeks.
}
\details{
A week is settled when it is at least \code{max_delay_weeks} ISO weeks older than the
newest week of \code{previous}. A row in \verb{$integrity} means that a published number
for a settled week changed, so ideally that table is empty. It compares finite
values only. \verb{$signal} counts a new week as a change. Both tables use the
median, \code{q = 0.5}.
}
\examples{
# 40 reference weeks, each reported 3, 10 and 17 days after its Monday
mondays <- as.Date("2023-01-02") + 7 * (0:39)
set.seed(1)
d <- data.table::data.table(
  isoyearweek_reference = format(rep(mondays, each = 3), "\%G-\%V"),
  reporting_date = rep(mondays, each = 3) + rep(c(3, 10, 17), 40),
  numerator = rpois(120, c(30, 15, 5)),
  indicator_tag = "x", location_code = "nation", age = "total", sex = "total"
)
d <- d[reporting_date <= mondays[40] + 6]

id <- c("indicator_tag", "location_code", "age", "sex")

# set the seed inside `run()`, so the two runs differ only by the data
run <- function(x) {
  set.seed(2)
  ens_collapse(nowcast_delay_ecdf_v1(
    csfmt_reporting_triangle_v3(x, id_cols = id),
    max_delay_days = 21, n_sim = 200
  ))
}

cur <- run(d)
d_prv <- data.table::copy(d)
d_prv[
  isoyearweek_reference == format(mondays[11], "\%G-\%V"),
  numerator := numerator - 5
]
prv <- run(d_prv)

# the engine horizon is 21 days, and this horizon is 3 weeks
qc <- qc_week_over_week_v1(cur, prv, max_delay_weeks = 3)

# the corrected week is settled, so its changed median is listed here
qc$integrity

# empty: these runs hold no status column, because neither
# mem_thresholds_v1() nor signal_detection_hlm() ran
qc$signal
}
\seealso{
\code{vignette("pipeline", package = "csalert")}, section 9.

Other quality control functions:
\code{\link[=compare_results]{compare_results()}},
\code{\link[=qc_surveillance_data_v1]{qc_surveillance_data_v1()}}
}
\concept{quality control functions}
