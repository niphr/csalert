# Validate a csfmt_ensemble_v3's invariants

Validate a csfmt_ensemble_v3's invariants

## Usage

``` r
validate_ensemble(ens)
```

## Arguments

- ens:

  A \`csfmt_ensemble_v3\`.

## Value

\`ens\` invisibly; errors on violation.
